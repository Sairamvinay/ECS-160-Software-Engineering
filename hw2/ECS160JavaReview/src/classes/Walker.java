package classes;

public interface Walker {
	
	public void walk(String location);
	
	//Cannot use protected or private modifiers in a interface.
	//protected void run(String location);
	
	//private void sprint(String location);

}
